## To Run the Program:

Create virtual environment
```sh
python3 -m venv venv
```

Activate virtual environment
```sh
source path/to/venv/bin/activate
```

Install Dependencies
```sh
pip install -r requirements.txt
```

Run Code
```sh
python3 main.py
```

If successful faculty information will be saved to **faculty_directory.csv**
